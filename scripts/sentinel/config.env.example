# Azure Sentinel and Log Analytics Configuration
# Copy this file to config.env and fill in your values

# ========================
# Azure Tenant Configuration
# ========================

# Target Azure tenant ID (required)
AZURE_TENANT_ID="00000000-0000-0000-0000-000000000000"

# Target Azure subscription ID (required)
AZURE_SUBSCRIPTION_ID="00000000-0000-0000-0000-000000000000"

# Service principal credentials (required for authentication)
AZURE_CLIENT_ID="00000000-0000-0000-0000-000000000000"
AZURE_CLIENT_SECRET="your-service-principal-secret-here"  # pragma: allowlist secret

# ========================
# Log Analytics Workspace Configuration
# ========================

# Workspace name (auto-generated if not provided)
# Default: {tenant_short}-sentinel-law-{region}
WORKSPACE_NAME="my-sentinel-workspace"

# Resource group for workspace (auto-generated if not provided)
# Default: rg-sentinel-{region}
WORKSPACE_RESOURCE_GROUP="rg-sentinel"

# Azure region for workspace
# Options: eastus, westus, westeurope, northeurope, etc.
WORKSPACE_LOCATION="eastus"

# Workspace pricing tier
# Options: Free, PerGB2018, PerNode, Premium, Standalone
WORKSPACE_SKU="PerGB2018"

# Log retention in days (30-730)
WORKSPACE_RETENTION_DAYS="90"

# Daily ingestion cap in GB
WORKSPACE_DAILY_CAP_GB="1"

# ========================
# Sentinel Configuration
# ========================

# Enable User and Entity Behavior Analytics (true/false)
ENABLE_UEBA="true"

# Enable Entity Analytics (true/false)
ENABLE_ENTITY_ANALYTICS="true"

# Sentinel content packs to install (JSON array)
# Available: AzureActivity, AzureAD, DefenderForCloud, ThreatIntelligence, O365, WindowsFirewall
SENTINEL_CONTENT_PACKS='["AzureActivity", "AzureAD", "DefenderForCloud"]'

# ========================
# Data Connectors Configuration
# ========================

# Data connectors to enable (JSON array)
# Available: AzureActivity, AzureActiveDirectory, Defender, ThreatIntelligence, O365
DATA_CONNECTORS='["AzureActivity", "AzureActiveDirectory", "Defender"]'

# ========================
# Diagnostic Settings Configuration
# ========================

# Diagnostic setting name
DIAGNOSTIC_SETTING_NAME="diagnostic-to-sentinel"

# Log categories to collect (JSON array)
# Use ["AllLogs"] to collect all available categories
# Or specify specific categories: ["AuditEvent", "Operational"]
DIAGNOSTIC_LOG_CATEGORIES='["AllLogs"]'

# ========================
# Resource Discovery Configuration
# ========================

# Resource discovery strategy
# Options: neo4j (use Neo4j graph), azure_api (query Azure API directly)
RESOURCE_DISCOVERY_STRATEGY="neo4j"

# ========================
# Execution Options
# ========================

# Strict mode: fail fast on first error (true/false)
STRICT_MODE="true"

# Dry run mode: show planned actions without executing (true/false)
DRY_RUN="false"

# Output directory for logs and status files
OUTPUT_DIR="./output"

# Debug mode: enable verbose logging (true/false)
DEBUG="false"
